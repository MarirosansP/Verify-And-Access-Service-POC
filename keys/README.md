# Keys Directory

Place your Concordium verifier account export file here:

    keys/verifier_account.export

This file is mounted read-only into the verifier container.

⚠️  **Do NOT commit real key files to Git.**
The `.gitignore` is configured to exclude them.
